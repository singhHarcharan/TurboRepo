{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "build:esbuild": "esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js --external:./node_modules/* --external:@repo/*",
    "dev": "tsc -b && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@repo/common": "*",
    "@types/express": "^5.0.6",
    "esbuild": "^0.27.2",
    "express": "^5.2.1"
  }
}
